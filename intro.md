面向 QQ 官方机器人的群管理插件：基于 AstrBot 已配置 LLM 的群消息审核与处置、入群申请智能审批、
成员与黑名单管理、申诉闭环，配套 SQLite 审计库与 WebUI 管理台。

## 内容审核
- 审核开启 / 审核关闭 ─ 开关本群审核
- 审核模式 严格/标准/宽松/仅记录/跟随 ─ 整体调节处置强度
- 审核阈值 0.0-1.0 ─ 送审风险分阈值
- dry-run 开启/关闭 ─ 演练模式：只记录 + 警告，不撤回 / 不禁言
- 关键词 列表/添加/删除 ─ 本地硬 / 软规则词表
- 审核日志 5 ─ 查看最近审核记录
- 审核统计 今日 ─ 送审量、处置分布与命中类型

## 变体识别（广告号防绕过）
- 形近字、拆字、繁体、插符号、空格、全角、拼音统一还原后再判定
- 9 条广告模板 × 104 条形近字表 × 41 个引导动作词，命中只代表"值得送审"，最终由 LLM 判定

## 入群审批
- 入群审核 开启/关闭/模式 ─ 四种模式：关闭 / 严格 / 标准 / 转人工
- 入群申请 / 入群通过 / 入群拒绝 ─ 查看与人工处理申请
- 判定链：本地黑名单 → 跨群黑名单 → 机器人账号 → 平台风险提示 → 信任邀请人 → LLM

## 成员与群务
- 禁言 @某人 10分钟 / 解禁 @某人 ─ 手动禁言与解禁（管理台同样可操作）
- 信任 / 取消信任 @某人 ─ 审核豁免名单
- 黑名单 列表/添加/移除 ─ 单群黑名单
- 全局拉黑 @某人 / 全局解除 @某人 / 全局拉黑 ─ 跨群黑名单（仅 AstrBot 管理员），命中后任意群拒绝入群
- 撤回 ─ 回复消息后撤回
- 群信息 ─ 本群档案与机器人群内状态
- 审核状态 ─ 审核开关、生效模式与自己的豁免状态

## 申诉
- 申诉 <理由> ─ 回复被处置的消息发送，按 Reply 精确关联到原审核事件
- 申诉通过 / 申诉驳回 ─ 群管处理申诉（与管理台「申诉」视图等价）
- 处理结果：原事件若产生过禁言会自动解禁、追加动作留痕、回执申诉人，不删除原审核记录

## 主动行为（无需指令）
- 入群申请轮询 ─ 定时拉取新申请并按判定链自动审批（游标 + 幂等去重，重启不重复审批）
- 禁言台账对账 ─ 每 5 分钟与平台对账，平台解禁后本地台账同步
- 平台能力探测 ─ 定时探测撤回 / 禁言等能力是否可用，受限时留痕并降级
- 审计库维护 ─ 定时清理过期记录并写入日统计
- 群管理自检 ─ 逐项探测平台能力并给出建议（仅 AstrBot 管理员）

## 管理台与配置
- WebUI 9 个视图 ─ 总览 / 群管理 / 日志中心 / 工具 / 策略 / 关键词 / 规则增强 / 成员与禁言 / 入群审批
- 「规则增强」自带判定测试 ─ 粘贴文本即可看到是否送审、风险分构成与命中依据（不发消息、不花 token）
- 竞赛域名白名单 ─ 内置竞赛站点默认降权（只降权、不豁免，仍走规则与送审）
- 图片送审可选 ─ 关闭 / 仅图文混排 / 纯图也送；二维码内容会交给模型识别并留痕
## 支持平台
- QQ 官方：qq_official / qq_official_webhook
- QQ 非官方：aiocqhttp（OneBot v11）

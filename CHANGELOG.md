# 更新日志

本文件记录所有值得注意的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

图例：✨ 新增 · 🐛 修复 · ⚙️ 变更 · ⚠️ 升级注意

---

## [0.13.2] - 2026-09-26

> 🐛 修复：OneBot 通道把 aiocqhttp 的「魔法属性」当成机器人 QQ 号，协议端刷 Uin2Uid Error。

### 🐛 修复
- _self_id() 不再直接依赖 getattr(bot, "self_id")：aiocqhttp 的 Api.__getattr__ 对任意未知属性
  返回 partial(call_action, 名字)，于是这个可调用对象被当成 QQ 号传给 get_group_member_info
  （生产 NapCat 日志：Uin2Uid Error: 用户ID functools.partial(...) 不存在）。
  现在只接受字符串/整数，取不到就返回空。
- 新增 _ensure_self_id()：缓存缺失时向协议端要一次 get_login_info 并记下来；
  仍取不到就不发 get_group_member_info，能力标记为「无法判定」，不再误判为失败/受限。
- 影响：机器人群内角色与管理员能力不再误判，协议端不再被此类错误日志刷屏。

### ✅ 验证
- 新增 3 条用例（模拟 aiocqhttp 魔法属性 / 优先用事件缓存 / 取不到时跳过探测），
  qqgm 全量 335 条用例通过。

---

## [0.13.1] - 2026-09-26

> 🐛 修复：官方通道的群名一直显示「（未获取群名）」——补上主动拉取与后台入口。

### 🐛 修复
- **官方群名从来没被获取过**：`ensure_group(name=…)` 的群名只可能来自 OneBot 事件的
  `abm.group.group_name`（官方适配器不带群名），而唯一会调 `get_group_info` 的只有「群信息」命令，
  拿到名字**用完即弃、从不回写**。于是后台群列表对官方群恒显示「（未获取群名）」，
  只能靠人工添加群时手填一次。现在补上闭环。
- 新增 `src/group_names.refresh_group_names()`：只挑 `name` 为空的群，逐个走
  `channel_for(platform_id).get_group_info()`（官方 → 开放接口 `GET /v2/groups/{openid}/info`，
  OneBot → `get_group_info`）并回写落盘；单群失败不影响其他群，返回值含 updated/failed/skipped/pending。
- 新增接口 `POST /astrbot_plugin_qq_group_manager/groups/refresh-names` 与后台
  「群管理 → 刷新群名」按钮。
- 新增 5 条测试覆盖：只补缺名群、全有名时空转、失败不中断、limit 截断、路由注册。

### ⚙️ 变更
- 群列表卡片说明补充：官方通道的群名需要点「刷新群名」主动获取。

---

## [0.13.0] - 2026-09-26

> ✨ 入群审批新增「入群答案校验」与后台问答展示，支持口令/暗号类硬门槛。

### ✨ 新增
- **入群答案校验**（后台「入群审批」页）：`join_expected_answer`（子串）/ `join_answer_keywords`（任一命中）/ `join_answer_regex`（正则）三项规则，**全空即不校验**（行为与旧版一致）。答案来源为申请人提交的 `review_qa_list[].answer` 与 `verify_message`。
- 未通过时的动作 `join_answer_action`：`manual` 转人工（默认，避免误伤）/ `decline` 自动拒绝 / `pass` 放行；`join_answer_case_sensitive` 控制是否区分大小写；正则写错按未配置处理，不会误拦人。
- 规则同时写入 LLM 提示词的【入群答案要求】，让模型参考同一门槛。
- **后台展示申请人完整问答**：入群审批列表的「验证消息 / 问答」列展示 `问：… → 答：…`，人工审批可直接核对。

### ⚙️ 变更
- 入群审批设置页新增「答案校验」区块（期望答案 / 关键词 / 正则 / 动作 / 大小写）。

---

## [0.12.0] - 2026-09-26

> ✨ 入群审批新增「申请人画像」：OneBot 通道可取 QQ 等级、注册时间/账号年龄、QID 与头像，
> 支持账号门槛与头像多模态复核；官方通道没有这些字段，按「无数据源」跳过，行为与旧版一致。

### ✨ 新增
- **申请人画像采集**：OneBot 通道用 `get_stranger_info` 取昵称、QQ 等级（`qqLevel`）、注册时间（`reg_time`）、QID、性别、VIP；头像由 QQ 号拼 `q1.qlogo.cn` 直链。
- **画像硬规则门槛**：`join_min_account_days`（账号年龄）、`join_min_qq_level`（QQ 等级）、`join_require_qid`，命中动作可选 `decline / manual / pass`（`join_gate_action`）。
- **资料缺失策略** `join_profile_missing`（`pass / manual / decline`，默认 `manual`）：只对**可能提供画像的通道**（OneBot）生效；官方/未知平台没有数据源，直接跳过，保证升级零行为变更。
- **头像多模态复核** `join_avatar_review`（`off / approve_only / always`）：把头像 URL 作为图片送审，识别擦边图与引流二维码；只允许把「拟放行」收紧为拒绝，模型不可用时静默降级。
- **画像缓存与限频**：`join_profile_cache_days / join_profile_qpm / join_profile_concurrency`，连续失败自动熔断 300 秒；只缓存成功画像。
- **管理台**：「入群审批」视图新增配置区（含此前只能在配置里改的全局模式、置信度门槛、轮询间隔等），待审/历史表新增头像、QQ 等级、账号年龄列；画像缺失时显示「本通道不支持」。
- **LLM 调用闸门真正生效**：单群 QPM（令牌窗口，超限先等待、等待上限内仍满则跳过本次判定）与全局并发（信号量），内容审核与入群审批共享同一套闸门与日预算/熔断。
- 入群审批的 LLM 调用接入与内容审核**共享的日预算与熔断**闸门。

### 🐛 修复
- **`llm_qpm_per_group` / `llm_max_concurrency` 此前从未生效**：两个配置项在默认值、范围校验与 WebUI 里都存在，但 `src/moderator.py` 里只有一个从未被使用的 `_semaphore` 空壳字段，限频与并发**从未实现**（管理台怎么调都没用）。现已补全，并纳入统计（`skipped_by_qpm` / `rate_waits` / `peak_concurrency`）。
- **OneBot 入群申请昵称恒为空**：加群请求事件本身不含 `username`，此前直接读该字段导致昵称为空；现在通过 `get_stranger_info` 补齐。

### ⚙️ 变更
- `join_requests` 表新增画像列（`profile_source / avatar_url / qq_level / account_age_days / reg_time / qid / profile_json / gate`），老库启动时自动补列。
- 判定结果新增 `gate` 字段（`account_age / qq_level / qid / profile_missing / avatar`），便于审计与申诉回溯。
- `docs/设计方案.md` 中「是否新号」的表述已修正：官方通道无账号年龄数据源。

### ⚠️ 升级注意
- 所有新增阈值**默认关闭**（`0 / off / manual`），升级后行为与 0.11.x 一致；官方通道不会产生任何画像相关请求。
- `qqLevel` / `reg_time` 是 NapCat 扩展字段，换协议端可能拿不到；拿不到时按「资料缺失策略」处理，不会误判为可疑。
- 画像属个人信息，随 `retention_join_days`（默认 90 天）一并清理。
- **真机验证（NapCat v4.18.28 + QQ 3.2.32，2026-09-26）**：`get_stranger_info` 确实返回 `qqLevel`（整数，如 11/53）与 `reg_time`（Unix 秒，如 1641000818），`nickname` / `long_nick` / `sex` / `age` / `is_vip` / `vip_level` 均可用；`login_days` 恒为 0（官方已废弃）；`q1.qlogo.cn` 头像直链返回 640×640 合法 JPEG。
- ⚠️ **`qid` 对真实账号普遍为空字符串**（实测机器人自身与普通账号都是 `""`）：不要开启 `join_require_qid`，否则会拒绝所有申请人。

---

## [0.11.1] - 2026-09-23

> 🐛 补齐三处漏判：全角/拉丁拼音写法、`扣群`口语写法、无外链的软性开群话术。
> 语料回归：正样本送审率 **96.9% → 100%**（漏判 3 → 0），负样本误送审率保持 **0%**。

### 🐛 修复
- **全角字母折叠后仍是拉丁**：`ｊｉａ群领资料` 经 NFKC 变成 `jia群领资料`，但 `jia` 没有映射回汉字，骨架视图看不见"加群"→ 整条消息零信号、不进送审。新增 **拉丁拼音/缩写别名表**（`jiaqun`/`kouqun`/`jia`/`qun`/`ling`/`kou`/`wx`/`vx` …），按**词边界**折叠，不会拆坏 `wxid_abc123` 这类标识符。
- **`扣群` 不算真实渠道**：`叩群领资料 秒通过` 的骨架已经是 `扣群领资料秒通过`，但 `扣群` 不在 `CHANNEL_MARKERS` 里，`ad_invite` 的 `require_channel` 把模板整条挡掉。已把 `扣群`/`叩群` 计入渠道标记（`叩`/`抠` 会先折叠成 `扣`）。
- **无外链的软性开群话术零命中**：`新群开张，福利多多，手慢无` 只有 40 分（单个黑话标记），低于 60 送审阈值。新增模板 `ad_open`（开群福利引流），要求 **开群场景词 × 诱饵资源词 × 催促词** 三个分组同时成立。

### ⚙️ 变更
- 新增词表：`OPEN_SCENE_WORDS`（新群/开张/开业/新开/建群）、`OPEN_URGE_WORDS`（手慢无/秒通过/先到先得/名额有限/抓紧 …）。
- `ad_open` 是唯一 `require_channel: False` 的模板，因此**刻意不收**"本群/群内/群里"这类弱场景词——实测 `本群资料限时开放，抓紧` 会因此被误送审。
- 内置模板 14 → 15 条，渠道标记 34 → 36 个，新增 12 条拉丁拼音别名。

### ⚠️ 升级注意
- `ad_open` 命中**只送审、不直接处置**（`action: ["warn"]`，且模板命中一律不自动执行撤回/禁言），因此不会新增误伤，只会增加少量 LLM 调用。
- 正常群公告若同时含"新群/开张"+"福利/资料"+"限时/抓紧"，会被送去 LLM 复核；如觉得吵，可在管理台「规则增强」里停用 `ad_open`。

---

## [0.11.0] - 2026-09-23

> ⚙️ 判定从"按字面/格式打分"改为"读懂意图"：送审时附带群聊上下文，提示词改为先推理后结论，并要求逐字引用证据。

### ✨ 新增
- 送审时附带最近 N 条群消息作为语境（`llm_context_messages`，默认 8，0 = 关闭）。模型据此判断"接梗/复读/吐槽/讨论/引用"，不再只看孤立的一条消息。
- 提示词 JSON 契约新增 `analysis`（先写判断过程）与 `evidence`（逐字摘录的判断依据）；两者随判定结果落审计，便于复核与调参。
- user 模板新增 `{context}` / `{context_count}` 占位符。

### ⚙️ 变更
- 内置 system 提示词重写：去掉十来个硬编码例句，改为可泛化的判断顺序（先问"作者想让读者做什么"→ 三项证据是否齐备 → 语境是否说明是玩梗 → 拿不准就 review），只保留一正一反两个对照样例。
- user 模板调整：群聊上下文与正文前置；"可疑点"移到正文之后并标注"本地规则误报率高，不构成违规证据"，避免先入为主。
- `llm_context_messages` 纳入数值边界钳制（0~20）。

### ⚠️ 升级注意
- 语境缓冲仅存内存（每群最多 20 条），重启即清空，不写审计库、不受 `store_text` 影响。
- 自定义过 `prompt_system` / `prompt_user` 的部署不受影响（仅留空时使用内置默认）；想用新模板需清空自定义提示词。

---

## [0.10.1] - 2026-09-22

> 🐛 协议端离线时不再刷屏告警。

### 🐛 修复
- OneBot 通道新增「连接状态」判断：协议端未连接时能力探测直接短路返回，不再对每个群各刷 3 条 WARN。
- call_action 失败日志降为 DEBUG，并在异常信息为空时补上异常类型（原来只显示「调用失败：」，看起来像崩溃）。

---

## [0.10.0] - 2026-09-22

> QQ 双通道适配：新增 OneBot v11（aiocqhttp）支持，官方族行为零回归。

### ✨ 新增
- 新增 src/platforms/ 通道抽象层：官方族（qq_official / qq_official_webhook）与 OneBot（aiocqhttp）。
- OneBot 通道：撤回（delete_msg）、禁言（set_group_ban）、踢人（set_group_kick）、群信息/成员列表、能力探测。
- OneBot 入群申请改为 request 事件驱动，审批走 set_group_add_request。
- 群配置新增 platform_id 字段，定时任务按平台实例路由。

### ⚙️ 变更
- support_platforms 增加 qq_official_webhook、aiocqhttp。
- 官方族沿用原有 QQGroupAPI 与 botpy 传输层，行为不变；OneBot 无原生黑名单时降级为「踢出 + 本地名单」。

### 🧪 真机验证
- 已用 NapCat（OneBot v11）端到端验证：群档案、撤回（delete_msg）、禁言/解禁（set_group_ban）、黑名单（set_group_kick）、入群申请事件、入群通过（set_group_add_request）。
- 联调修复：入口装饰器放宽到 aiocqhttp；OneBot 通道 get_group_info/get_bot_state 返回 GroupProfile/BotState（修复「群信息」报错）。

---

## [0.9.1] - 2026-09-18

> 📖 提供 `intro.md`：安装「菜单导航」插件后，发送 `插件介绍` 即可看到本插件的完整功能清单。

### ✨ 新增
- 根目录新增 `intro.md`，按功能分组列出全部能力，**包含菜单里看不到的主动行为**
  （入群申请轮询、禁言台账对账、平台能力探测、审计库维护）。

### ⚙️ 变更
- 仅新增文档文件与说明，**无任何功能/行为变更**，可平滑升级。

---

## [0.9.0] - 2026-09-18

> ⚖️ 补上审核插件最大的痛点：**申诉闭环**（Reply 精确关联 → 人工复核 → 自动解禁 + 留痕），
> 并新增**竞赛域名白名单**（只降权、不豁免）、**二维码内容识别**与**跨群黑名单**；
> 顺带修掉了"回复被处置消息"失效与审计库关闭竞态两个老问题。

### 🐛 修复
- **`申诉` 的"回复被处置消息"此前完全失效**：旧实现把事件对象丢弃（`del event`），
  关联只能靠"最近一条被处置记录"猜测。现在按 Reply 精确关联到原审核事件，
  取不到引用时回退旧行为，并在日志里说明回退原因。
- **审计库关闭竞态**：`AuditStore.close()` 原先在 cancel 写协程后立即关闭连接，
  极端时序下会报 `cannot commit - no transaction is active`；现改为等写协程自然退出（超时才 cancel）。

### ✨ 新增
- **申诉闭环**：
  - 成员**回复被处置的消息**发送 `申诉 <理由>`；新增群管指令 `申诉通过` / `申诉驳回`（可带理由）。
  - 管理台新增「申诉」视图（时间 / 群 / 申诉人 / 理由 / 原判 / 状态 + 通过 / 驳回 + 查看原文），
    日志页新增"已申诉"筛选与处理状态列。
  - 申诉通过时：原事件若产生过禁言则**自动解禁**并同步禁言台账，追加动作留痕，
    **不删除、不改写原审核事件**；同时回执申诉人、通知管理员会话。
- **误判自学习白名单（默认关闭）**：打开后，申诉通过的消息摘要进入白名单，
  后续同内容消息**跳过 LLM 送审**但仍写审计（规则分与处置矩阵不变）；
  管理台「规则增强」页可查看与撤销，并展示"被申诉通过最多"的规则，作为规则优化建议。
- **竞赛域名白名单**：内置竞赛站点（牛客 / Codeforces / AtCoder / 洛谷 / XCPC 等）默认降权——
  白名单内链接不计链接分、不置"含链接"标记，但**仍走完整规则、审计与 LLM 送审判断**（只降权，不豁免）；
  混入任意非白名单链接时按"有链接"保守处理，避免成为绕过审核的后门。管理台「策略」页可编辑。
- **二维码内容识别（复用多模态，零新依赖）**：图片送审时要求模型把二维码承载文本填入 `qr_text`；
  命中群号 / 短链 / 链接等引流特征时**升级严重度并在审计中留痕**
  （不覆盖模型自身的判定，避免把"识别失败"当成违规）。
- **跨群黑名单**：新增 `全局拉黑 @某人 <理由>` / `全局解除 @某人` / `全局拉黑`（查看列表，仅 AstrBot 管理员）
  与管理台「成员与禁言 → 跨群黑名单」区块；命中后**任意群的入群申请都会被自动拒绝**，
  默认不自动移出已在群成员。入群判定链：本地黑名单 → **跨群黑名单** → 机器人账号 →
  平台风险提示 → 信任邀请人 → LLM。
- **新增配置项**：`domain_allowlist_enabled`（默认开）、`domain_allowlist`（内置竞赛域名）、
  `appeal_enabled`（默认开）、`appeal_auto_whitelist`（默认关）、`appeal_notify`（默认开）。
- 新增 `summary_by_category(days)` / `summary_by_group(days)` 聚合，为后续审核周报做准备。

### ⚙️ 变更
- `main.py` 的 `VERSION` 与 `metadata.yaml` 统一为 `0.9.0`（此前分别为 `0.5.0` / `0.8.0`）。

### ⚠️ 升级注意
- **老库会自动补列**：启动时用 `PRAGMA table_info` + `ALTER TABLE ADD COLUMN` 幂等补
  `mod_events.appeal_by / appeal_at / appeal_note`，并新建 `appeal_whitelist` 表；
  迁移失败只记 warning、不阻塞启动。**升级前建议先备份审计库**。
- 白名单只降权 / 只跳过送审，**不会豁免规则命中与处置矩阵**；误判自学习白名单默认关闭。
- 管理台新增两个视图（「申诉」「成员与禁言 → 跨群黑名单」），刷新页面后可见。

### ✅ 验证
- 全量 **234 passed**（新增 `tests/test_appeal.py` 14 例、`tests/test_global_blacklist.py` 8 例、
  `tests/test_rules.py` 6 例、`tests/test_ui_contract.py` 3 例、审核链路 2 例）。

---

## [0.8.0] - 2026-09-16

> ✨ 补齐"繁体 + 异体字 + 全角 + 空格插字"这类重度混淆：实测保健药广告与非法荐股广告
> 修复前评分 **0（完全漏检）**，现在分别命中 55 / 100 分。

### ✨ 新增
- **繁→简字符表（离线生成，运行时零依赖）**：新增 `src/simplify.py`，
  用 `opencc -c t2s.json` 对 CJK 基本区/扩展A/兼容表意区**逐字**转换，
  取一对一映射共 **3751 字**，编译为 `str.maketrans` 两张字符串表（25KB）。
  在归一化阶段（NFKC 之后、去干扰符之前）执行，因而
  `進媺國原裝偉哥讓沵變佷侽亾` 先变成 `进媺国原装伟哥让沵变佷侽亾` 再匹配。
- **异体/仿造字表扩充**（OpenCC 不覆盖的那批）：新增 60+ 条，含
  `侽→男 亾→人 哋→的 沵→你 佷→很 媺→美 兲→天 嘫→然 莋→作 鼡→用 吢→心 芣→不
  噫→意 佲→名 苝→北 炷→注 嫃→真 盆→朋 伖→友 玖→久 萣→定 珂→可 収→收 岀→出
  甴→由 沩/潙→为 対→对 哯→现 巳→已 浉/溮→师 㳡→过 孖→子 ㄧ→一 仴→月 杺→心 悝→理`
  等。骨架视图现在能还原成整句：
  `男人的secret进美国原装伟哥让你变很男人纯天然无副作用不满意`。
- **渠道视图（保留分隔符）**：新增 `channel_view`（只去空白与各种括号、**保留冒号**）
  用于联系方式/链接识别。此前 `V：\n[ t e s t _ i n v e s t ]` 会被 compact 连冒号一起删掉
  （→ `vtestinvest`），两种视图都识别不出联系方式。
- **两个新模板**：
  - 「药品保健品广告」（伟哥/壮阳/延时/一粒见效/无副作用/保密发货/无效退款/原装进口/
    男用/增大/印度神油… + 电话/订购/发货/包邮/咨询…）
  - 「荐股理财诈骗」（内幕消息/带盘/实盘验证/出金/锁仓/财务自由/月收益/稳定收益/
    名额有限/仅限/荐股/拉升/建仓/私募… + 收益/理财/股票/投资/名额/备注/一对一…），
    并设 `require_channel`，避免把"内幕""名额有限"这类日常词误判。

### ✅ 验证
| 样本 | 修复前 | 修复后 |
| --- | --- | --- |
| 保健药广告（侽亾哋 𝕤𝕖𝕔𝕣𝕖𝕥 / 媺國原裝偉哥 / 純兲嘫無副莋鼡） | **0 分** | 55 分（药品保健品广告） |
| 非法荐股广告（內募消息 / 帶盤 / 實盤驗証 / 財務自由 / 僅限10人 / V:test_invest） | **0 分** | **100 分**（荐股理财诈骗 + contact） |
| 繁体正常聊天（這個題目我寫完了 / 請問複雜度是多少） | 0 分 | **0 分（无误报）** |
| 家教帖 / 兼职刷单 / 赌博引流 | — | 放行 / 100 / 100（能力未回退） |

- 新增 `tests/test_obfuscated_spam.py`（7 条）；全量测试 **198 passed**
  （3 个失败为改动前既有的环境问题）。

## [0.7.1] - 2026-09-16

> ⚙️ 招聘/家教豁免不再是"免死金牌"：**同一文案被多个不同成员群发**时按群发广告处理。

### ⚙️ 变更
- 新增 `recruit_mass_send` 判定：招聘/家教类文案在**同一窗口内被 ≥N 个不同成员**发送时，
  **不再走豁免**，而是照常进入送审与处置流程，并在信号里注明原因。
  - 复用引擎既有的 `note_content()`（按文案骨架去重、统计**不同成员数**），
    无需新增存储；窗口与阈值沿用现有配置
    `duplicate_flood_window`（默认 300 秒）与 `duplicate_flood_members`（默认 3）。
  - 判定顺序：先算"像招聘内容"（结构化标记 ≥2、≥30 字、无外链、无灰产诱饵），
    再分两路 —— 命中多人群发 → `recruit_mass_send`(+35) 不豁免；
    否则 → `recruit_context` 豁免（分数 ≤20、撤销广告模板、跳过 LLM 复审）。
- 于是"个人发一条家教帖"与"中介扫群群发"被区分开：
  | 场景 | 行为 |
  | --- | --- |
  | 1 个成员发家教帖 | 豁免放行（≤20 分） |
  | 同一人连发多次（成员数仍为 1） | 豁免放行 |
  | 2 个成员发同一文案（未达阈值 3） | 豁免放行（避免误伤转发） |
  | **3 个成员发同一招聘/家教文案** | **照常处置**（score 100/95，含 duplicate+recruit_mass 信号） |

### ✅ 验证
- 新增 4 条断言（群发不豁免、正当家教帖被群发同样处置、单人重复仍豁免、
  未达成员阈值仍豁免）；全量测试 **191 passed**（3 个失败为改动前既有的环境问题）。
- 生产配置确认：窗口 300 秒、成员阈值 3（可在 WebUI 调整）。

## [0.7.0] - 2026-09-16

> 🐛 修复两类线上误杀：**数字连串判定错误**（把全文数字总和当长号码）与
> **家教/招聘信息被当成引流**。实测 4 条真实教学招聘信息全部放行，
> 4 条披着家教外衣的广告仍被拦截。

### 🐛 修复
- **`longest_digit_run` 判定错误（重要）**：旧实现先用 `digits_of()` 把全文数字
  **抽出来拼成一个纯数字串**，再在上面数"连续"数字 —— 等价于"全文数字总数"。
  于是任何含若干数字的正常消息（价格、年份、班号、学历"985"…）都被算成长号码：
  家教信息里 `220 + 985 + 666 + 50` 被算成 **24 位**，直接触发 `digit_run_long`（+60）。
  现在改为在归一化文本上**逐字符**扫描真正的连续数字串。
  `加群 998877` → 6 位（仍触发弱信号），`13812345678` → 11 位（仍触发强信号）。
- **家教/招聘信息被误判为广告引流**：群内 CS 学生接家教、学校招老师是正常内容，
  其中的"加微信 / 联系v:"是**信息本体**。此前会命中
  `拉群引流：加薇+网课`、`私聊引流：加微+网课`、`加我+加我微信` 等模板，
  加上 `contact`(+60) 与错误的 `digit_run`(+60) 直接判违规。
  新增**招聘/家教语境识别**：命中 ≥2 个结构化标记
  （辅导科目/学员情况/时间安排/教员要求/老师薪水/教员/学员/试课/招聘/代课/岗位/
  五险一金/本科及以上…）且正文 ≥30 字、**无外链**、**无灰产诱饵**时：
  分数压到 ≤20、撤销"广告引流/诈骗赌博"模板命中、标记 `recruit_context`，
  并在 `main.py` 中**跳过 LLM 复审**。

### ⚠️ 防滥用护栏
- 出现下列任一情况**不予豁免**：外链、`资源/福利/裸聊/约炮/看片/博彩/彩票/刷单/返利/
  垫付/外挂/破解/私服/彩金/首充/种子/磁力/防失联/备用群/内部群/免费领取/点击领取/
  扫码领取/加群领取/日结/代收/跑分`。
- 已验证 4 条伪装样本仍被拦截：`家教+资源`、`家教+免费领取`、`家教+外链`、`家教+色情诱饵`。

### ✅ 验证
| 样本 | 修复前 | 修复后 |
| --- | --- | --- |
| 家教-六年级奥数（含 `加微信:` / `联系v:`） | **100 分** 判广告引流 | 20 分 · recruit_context · 跳过复审 |
| 家教-幼儿园NOI | 100 分（contact+digit_run） | 20 分 · 放行 |
| 学校招聘（启明中学） | 0 分 | 0 分 · recruit_context |
| 家教中介群发（交换微信） | 90 分 判广告引流 | 20 分 · 放行 |
| 家教+资源 / +免费领取 / +外链 / +色情诱饵 | — | **仍 100/85 分拦截** |
| 兼职刷单 / 赌博引流 / 色情引流 | 命中 | **仍 100 分命中** |

- 新增 `tests/test_recruit_context.py`（12 条）并修正数字连串回归；
  全量测试 **187 passed**（3 个失败为改动前既有的环境问题）。

## [0.6.0] - 2026-09-16

> ✨ 依据公开研究系统性补齐 6 类绕过手法：新增 emoji 暗号、中英混排/leet、拆字、黑产黑话与放贷诈骗判定，
> 实测 12 条规避写法全部命中，12 条正常语境零误报。

### 参考来源
- 腾讯云开发者社区《敏感词过滤失效？揭秘拼音变体、谐音替代等 6 种高级文本绕过手法及防御策略》
  （拼音替换 / 谐音字 / 拆字偏旁 / 符号干扰 / emoji 编码 / 中英混排）
- 公开黑灰产黑话资料（跑分、水房、狗推、菜农、接码、四件套等）

### ✨ 新增（按研究逐条补齐）
- **手法六 中英混排 / leet**：新增 `leet_fold()` 折叠（`0→o 1→i 3→e 4→a 5→s 7→t @→a $→s`），
  `色q1ng→色qing`、`赌b0→赌bo`；拼音视图改为基于折叠后的文本计算，
  因此 `色q1ng` 也能算出 `seqing`；模板匹配同样会检查 leet 视图；
  新增 `leet_bypass` 信号（+45）用于原文规避、折叠后才现形的情况。
- **手法五 emoji 编码**：新增「emoji 暗号引流」模板
  （`🔞🍑🎰🃏👙🍆💦🎲🚗` 与诱饵/渠道词**共现**才算），避免误伤日常用表情的聊天。
- **手法三 拆字/偏旁**：`MULTI_PHRASES` 补充 `力口→加`、`贝者→赌` 等拆字组合。
- **黑产黑话**：新增「赌博黑话」模板（菠菜盘/bc盘/信用盘/包杀/包赢/龙虎/洗码/返水/狗推/狗庄/
  菜农/水房/接码/卡商/料商/四件套/跑分平台/代收代付 + 赌博平台/网站等具体组合），
  诱饵词表补充 `出款/下款/洗码/返水/内部号/带单/跟单/四件套/话费卡`。
- **放贷诈骗**：新增「放贷诈骗」模板（无抵押/秒下款/黑户可下/包装资料/征信修复/停息挂账…）。
- **加v 家族变体**：渠道词补充 `加vx/加wx/加qq/薇信/徽信/v信/抠抠/企鹅号/电报`。

### ⚠️ 设计约束（防误报）
- 菠菜（蔬菜）、上岸（考研）、跑分（性能测试）、水房、菜农、接码、四件套 等词**均有正常含义**，
  因此一律**只做组合判定**：必须与渠道/交易词共现才成立；
  黑话模板的第二组刻意**不含"平台/网站"**这类泛词，否则"接码平台是干什么的"会被误命中。

### ✅ 验证
- 12 条规避写法（emoji、leet、拼音、拆字、符号、黑话、放贷）**全部命中**；
- 12 条正常语境（吃菠菜、考研上岸、跑分测试、讨论接码平台、水房打水、菜农种菜…）**零误报**；
- 原有正/负语料回归全绿；全量测试 **177 passed**
  （3 个失败为改动前既有的环境问题，已与基线对比确认）。

---

## [0.5.2] - 2026-09-16

> 🐛 修复符号型规避写法漏检：`资料 ➕v shhdjdkl` 这类消息此前**完全不命中任何规则**。

### 🐛 修复
- **加号家族被当噪声删除，导致"加v"类广告漏检**：
  `➕`(U+2795) 落在 `_EMOJI_RE`（U+2600–U+27BF）区间被当表情删掉，`＋`/`﹢`/`+`
  又被 `_INTERFERENCE` 当干扰符删掉 → 归一化后 `资料 ➕v shhdjdkl` 变成 `资料vshhdjdkl`，
  规则层看不到"加v"，评分 0、不送审、不处置。
  现在在**去噪之前**把加号家族映射成汉字"加"（`➕ ✚ ＋ ﹢ ᐩ +` → `加`），
  于是 `资料➕v…` → 骨架 `资料加v…`，正常命中"拉群引流"模板与 `invite_bait` 信号。
- **`CONTACT_RE` 支持无冒号写法**：新增 `v/微/薇 + 分隔符 + 长 ID` 分支
  （如 `v shhdjdkl`、`微 shhdjdkl`），此前只认 `v:xxxx` 这种带冒号的写法；
  该分支要求别名前后不能紧邻字母数字，因此 `version`、`very good` 等不会误命中。

### ✅ 验证
| 输入 | 修复前 | 修复后 |
| --- | --- | --- |
| `资料 ➕v shhdjdkl` | 0 分、无命中 | **100 分**（拉群引流+召唤式引流，contact） |
| `资料+v shhdjdkl` / `＋v…` / `﹢v…` / `✚v…` | 0 分 | **100 分** |
| `资料➕vshhdjdkl`（无空格） | 0 分 | **100 分** |
| `＋v shhdjdkl 领资料` / `有资料 ➕薇 shhdjdkl` | 0 分 | **100 分** |
| `资料 v shhdjdkl`（无加号） | 0 分 | **60 分** + contact 信号 → 送审 |
| `C++ 和 Python 哪个好写`、`3+2=5`、`版本 v1.2.3`、`f(x)=x+1` | 0 分 | **0 分**（无新增误报） |

- 新增 `tests/test_plus_alias_bypass.py`（16 条）：9 种加号/形近写法必须命中、
  6 条含 `+`/`v` 的正常消息不得误报、骨架视图必须能看到"加v"；
  全量测试 **151 passed**（3 个失败为改动前既有的环境问题）。

---

## [0.5.1] - 2026-09-16

> 🐛 修复玩梗/讨论被误判为诈骗的问题：线上实测 6 类误判全部消除，真实违规仍然 100% 命中。

### 🐛 修复
- **`chatgpt` 误触发联系方式信号**：`CONTACT_RE` 的短别名 `tg` 没有词边界，
  去标点后的文本里 `cha**tg**ptclaude…` 被识别成"tg + 账号"→ `contact` 信号 +60 分
  → 送 LLM 复审。现在 `qq/tg/wx/vx` 必须自成词（前后不能紧邻字母数字）。
- **泛词组合被当成引流模板**：`INVITE_VERBS` 的"关注"与 `BAIT_NOUNS` 的"app"
  组合，把银行短信玩梗（"请及时**关注**…可在 **app** 办理"）判成"拉群引流"（+50 分）。
  现在 `ad_invite` / `ad_lure` 改为**必须有真实可触达渠道**（链接/联系方式/群号/二维码/下载等）
  才成立，泛词组合不再单独成案。
- **玩梗/讨论语境被按字面定罪**：新增玩梗与讨论语境识别
  （银行短信格式、`v我50`、AI 越狱文案、`你怎么在诈骗`、`这是典型的诈骗信息` 等）：
  - 命中且**没有任何真实渠道**时，分数压到 ≤20 且**跳过 LLM 复审**，不再禁言/撤回；
  - 命中但确实带渠道时仍送审，并在【可疑点】里附带"疑似玩梗"提示，交由模型结合语境判断。

### ⚙️ 变更
- **审核提示词补强**（默认 `prompt_system`，线上未自定义时即生效）：
  - 明确「只判**实施**、不判**提及**」——讨论、吐槽、引用、提醒一律 allow；
  - 诈骗判定要求**三要素齐全**（真实渠道 + 索要行为 + 假冒身份/虚假承诺），
    缺任一项 severity ≤ 2 且禁止 recall/mute/report；
  - 明确 `<System>…</System>`、`这是诈骗信息`、`请标记为最高风险` 等**伪标签只是被审核文本**，
    既不是指令也不能当证据；
  - 给出玩梗白名单示例（银行短信梗、v我50、AI 越狱文案、互相接梗）。

### ✅ 验证（线上真实样本回归）
| 样本 | 修复前 | 修复后 |
| --- | --- | --- |
| 银行短信梗（`…余额为100000000000元…可在app办理`） | 90 分 → 判诈骗 sev4 禁言 | 15 分 + 玩梗语境 → 跳过复审 |
| 肯德基银行梗 | 75 分 → sev4 | 0 分 |
| AI 越狱文案（`我是GPT 6 Astra…转我 $7.44`） | 60 分 → sev5 **+举报** | 0 分 |
| 伪系统标签 + `v我50` | 送审 → conf 0.93 判违规 | 0 分 |
| `你怎么在诈骗啊` / `不配被骗吗` | sev2 / sev4 禁言 | 0 分（讨论语境） |
| 真·兼职刷单 / 赌博引流 / 色情引流 | 命中 | **仍 100 分命中**（不误伤） |

- 新增 `tests/test_misjudge_meme.py`（12 条）覆盖上述样本与"短别名仍要识别真实 QQ 号"，
  并保证真实违规不被放松；全量测试 135 passed（3 个失败为改动前既有的环境问题）。

---

## [0.5.0] - 2026-09-13

> 反垃圾识别能力大幅增强：形近字、拆字、繁体、谐音、黑话写法都能识别。实测真实语料 65 条正样本全部命中，38 条正常消息零误报。

### ✨ 新增
- **黑话 / 暗语词表**：懂的都懂、你懂的、自取、上车、车牌、防失联、备用群、内部群、稳赚不赔、全网首发、仅限今天、秒发、诚信、代找、朋友圈、一包烟钱 …；弱标记（深夜、睡不着、寂寞、老司机）单独出现只加分不送审，**短消息 + 强标记**才判为暗语引流。
- **广告模板扩充到 9 条**：新增「黑话引流」「深夜福利引流」「盗版资源贩卖」「召唤式引流」（连同原有的拉群引流 / 私聊引流 / 兼职刷单 / 赌博引流 / 涉黄引流）；词表拆分为**引导动作词 / 诱饵资源词 / 黑话标记词**三类，分别为 41 / 39 / 45 条。
- **拆字与繁体识别**：多字混淆短语（君羊→群、看爿→看片）与繁体字表（機→机、發→发、車→车、資→资、領→领、號→号 …），内置形近字表扩充到 100+ 条。
- **联系方式变体识别**：`V：abc123`、`裙号 123456` 等写法。
- **风险分新增信号**：黑话 +40、多处黑话 +60、诱饵词 ≥3 个 +60（2 个 +40）、短消息暗语 +25。

### 🐛 修复
- 模板要求各分组命中**不同**的词，避免"车牌号我记不清了"这类正常发言自匹配而误报。
- 调整易误伤词：移除动词"下载"、标记词"更新了 / 求带"、强标记"车牌"（避免"公告我更新了""合集下载完了"被误判）。

### ⚠️ 升级注意
- 判定更严格、也更容易送审；若此前已关闭 dry-run，建议先切回观察一天。

---

## [0.4.2] - 2026-09-13

> 修复策略页"勾选后保存又变回默认"的问题。

### 🐛 修复
- 送审条件新增的「疑似联系方式 / 命中广告模板 / 含图片」未被后端白名单接受，保存时被静默丢弃 —— 已补齐。
- 保存成功后的提示会带上**实际生效的送审条件**，便于确认后端存了什么。

---

## [0.4.1] - 2026-09-13

> 修复拼音写法识别，并收紧送审条件语义：闲聊与普通链接不再消耗 token。

### 🐛 修复
- 拼音视图此前会丢弃非汉字内容，导致 `jiaqun ling ziliao` 这类纯拼音写法无法匹配。
- 规则的归一化匹配新增拼音兜底：`ｊｉａ群领资料`、`jiaqun ling ziliao` 均可命中 `加群领资料`。

### ⚙️ 变更
- `rule_hit` 只认"规则 / 模板命中"；链接、联系方式、长数字等内置信号改由 `risk>=N` 或独立条件参与判断，"闲聊 + 偶尔发链接"不再送审。
- 权重调优：联系方式 60、8 位以上长数字 60、图片 60（各自单独即达阈值）。

---

## [0.4.0] - 2026-09-13

> 引入变体识别体系：从"关键词匹配"升级为"归一化 + 模板 + 风险分"。

### ✨ 新增
- **文本归一化**：每条消息生成原文 / compact（去全角、去符号、去零宽）/ 骨架（形近字映射）/ 拼音视图，规则多视图同时匹配。
- **规则类型扩展**：`normalized`（新规则默认）/ `literal` / `regex` / `fuzzy`（编辑距离）/ `pinyin`（同音，需 pypinyin）。
- **风险分与送审条件**：本地信号累加 0–100，送审条件支持 `risk>=N`、`has_image`、`has_contact`、`ad_template`。
- **同文案多号刷屏检测**：同一骨架文本在窗口内被多个成员发送即计入风险分。
- **管理台「规则增强」视图**：广告模板与形近字表可视化维护 + **判定测试**（展示归一化视图、命中依据、风险分明细与是否送审）。
- **回归语料与评分机制**：正样本送审率、负样本误报率成为可量化指标。

### ⚙️ 变更
- 默认送审条件由 `rule_hit` 改为 `rule_hit + risk>=60`。
- 变体 / 模板命中**只送审**，是否违规交由 LLM 判定，处置按判定到的类别与严重度执行。

---

## [0.3.7] - 2026-09-13

### ✨ 新增
- **图片送审**（可选，默认关闭）：不送 / 仅图文混排 / 纯图片也送，图片以多模态交给同一个审核模型。

### 🐛 修复
- 此前纯图片消息会被直接跳过，图文混排也只审文字。

---

## [0.3.6] - 2026-09-13

### 🐛 修复
- **管理台保存关键词不生效**：保存路径绕过了规则引擎热更新，新加的动作要重载插件才生效（"撤回生效了、禁言没生效"的根因）。
- 审核判定日志现在同时打印"实际动作 / 计划动作 / 被拦下的动作"，一眼分清是规则没配还是被模式拦下。

---

## [0.3.5] - 2026-09-13

### 🐛 修复
- **群级模式覆盖导致全局改动不生效**：新建群记录不再复制当时的全局模式；历史覆盖可用 `审核模式 跟随` 清除。

### ✨ 新增
- 管理台「群管理」表格支持逐群选择审核模式（跟随全局 / 严格 / 标准 / 宽松 / 仅记录）。

---

## [0.3.4] - 2026-09-13

### ✨ 新增
- `dry-run 开启 / 关闭 / 查看` 指令，群内即可在"演练"与"实际处置"之间切换。

### 🐛 修复
- 违规动作被模式或 dry-run 拦下时群里毫无反馈 —— 现在会兜底补一条警告，并说明原因。

---

## [0.3.3] - 2026-09-13

### 🐛 修复
- **审核链路在真实运行时抛异常**：缺少一个内部方法，导致每条群消息都报错且静默无响应（表现为"发广告没反应"）。已修复并补上整链路集成测试。
- **dry-run 连警告都吞掉**：dry-run 现在只拦截破坏性动作（撤回 / 禁言 / 拉黑 / 移除），警告与上报照常发送；可用 `dry_run_warn` 完全静默。

---

## [0.3.2] - 2026-09-13

### 🐛 修复
- **管理台按钮点了没反应**：插件页面运行在受限 iframe 中，浏览器会忽略原生确认弹窗（直接返回 false），导致所有带二次确认的操作静默失效。已改为页面内自绘弹窗。
- **QQ 指令没反应**：指令执行异常时不再沉默，会回复失败原因；新增私聊提示（群管理指令只作用于群聊）。

---

## [0.3.1] - 2026-09-13

### ✨ 新增
- 管理台可**选择审核模型**（配置 `llm_provider_id`），留空则跟随会话默认模型；模型失效自动回退。

### 🐛 修复
- 提示词与刷屏阈值此前未在默认配置中声明，管理台保存会被静默丢弃。

---

## [0.3.0] - 2026-09-13

> 入群审批与成员管理上线，管理台补齐到 8 个视图。

### ✨ 新增
- **入群申请智能审批**：游标轮询 + 幂等去重；黑名单 / 机器人账号 / 平台风险提示 / 信任邀请人 / LLM 五级判定；`off / strict / standard / human` 四种模式。
- **官方入群策略透明化**：查看、启停、扫描、白名单管理，并与插件审批做冲突检测。
- **成员管理**：手动禁言 / 解禁（指令 + 管理台）、禁言台账与平台对账、黑名单双轨、批量移除、成员查询。
- **管理台新增视图**：「成员与禁言」「入群审批」。

---

## [0.2.0] - 2026-09-13

> LLM 审核与自动处置上线。

### ✨ 新增
- **本地规则层**：硬规则（命中即处置）/ 软规则（提升关注），支持正则与作用域；内置外链、联系方式、重复字符、超长、刷屏检测。
- **LLM 审核引擎**：严格 JSON 输出 + 置信度门槛；结果缓存、采样率、单群限速、并发上限、日预算、失败熔断。
- **处置执行器**：警告 / 撤回 / 禁言 / 拉黑 / 移除 / 上报，按 severity 矩阵执行，支持演练、幂等与能力降级。
- **申诉机制**：成员可对处置结果申诉，管理台可按"已申诉"筛选。
- **管理台新增视图**：「策略」「关键词」。

---

## 早期版本（0.1.0 – 0.1.x）

> 插件骨架、平台能力探测、审计库与管理台首版。

### ✨ 新增
- AstrBot Star 入口、服务装配、生命周期与后台任务调度（配置落盘、能力探测、审计维护、入群轮询、禁言对账）。
- **QQ 群管理 API 客户端**：覆盖群信息、群状态、入群审批、禁言、撤回、成员、黑名单与官方入群策略；令牌桶限频、指数退避重试、错误码语义映射、dry-run 拦截写操作、全量调用审计。
- **能力自检**：只用只读接口逐项探测平台能力，无只读接口的能力标记为"按需尝试"。
- **SQLite 审计库**：审核事件、动作、入群申请、API 调用、能力受限、禁言台账、日统计；异步批量写入、保留期裁剪、备份与导出。
- **管理台首版**：总览 / 群管理 / 日志中心 / 工具。
- **审核启用前置校验**：必须已开启「接收全部消息」，否则拒绝启用并给出手机 QQ 操作指引；后台周期复查，能力丢失自动暂停。

### 🐛 修复
- 平台 4xx 权限类错误被误判为服务端错误并触发重试风暴。
